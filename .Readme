# Build the Docker image
docker build -t flask-ml-app .

# Run the Docker container
docker run -p 5000:5000 flask-ml-app



#DVC

# Initialize Git and DVC
git init
dvc init

# Add the dataset to DVC
dvc add iris.csv

#Create a .dvc file to track the dataset.
#Move the dataset to the DVC cache and create a symbolic link in your workspace.

# Commit changes to Git
git add iris.csv.dvc .gitignore
git commit -m "Add Iris dataset to DVC"

# Configure remote storage
dvc remote add -d myremote s3://mybucket/path

# Push data to remote storage
dvc push